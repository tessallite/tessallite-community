# F-030-01 / Bug-9365: http-context import for the SPA /health fan-out.
# Included before default.conf (00- prefix) so js_content can resolve.
js_path /etc/nginx/njs;
js_import health_and from health_and.js;
