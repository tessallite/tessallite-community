Screenshot of the Model Builder Canvas showing three table cards — "orders" (fact, centre), "dim_product" (dim_aggregate, left), and "dim_date" (dim_aggregate, right) — with join lines drawn between them. The join line between orders and dim_product is selected and highlighted. The Drawer on the right shows the join form with: Left table: orders, Left column: product_id, Right table: dim_product, Right column: product_id, Join type: LEFT. "Save Join" and "Delete Join" buttons appear at the bottom of the Drawer. Dimensions: 1280x800px.