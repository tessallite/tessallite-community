Screenshot of the Import Model dialog after the user has loaded a .tessallite.json file. Title: "Import model from JSON". Body shows: a "Choose file" button (already-clicked, disabled state), the loaded bundle's slug and display name in editable TextFields, a "Rebind connections" subheader, and 2-3 connection rebinding rows. Each row shows the source's connection role + display name + connection_type on the left ("Source: analytics (postgresql)") and a Select dropdown on the right with the local connections. A "Deploy immediately after import" checkbox at the bottom (unchecked). Cancel + Import buttons. The Import button is enabled (all connections mapped). Dimensions: 800x700px.
