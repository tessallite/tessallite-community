Screenshot of the typed-name confirmation dialog that appears when deleting a high-risk object (model, project, or tenant). Title: "Delete model 'sales'?". Body text reads "This permanently removes the model and all its versions. There is no undo.". A consequences callout (orange border) lists the downstream impact: queries against this model will fail, scheduler stops refreshing aggregates, BI tools lose visibility. A TextField labeled "Type 'sales' to confirm" with a partially-typed value. The Delete button (red) is disabled because the typed text does not match yet. Cancel button on the left. Dimensions: 600x450px.
