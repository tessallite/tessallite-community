Screenshot of the Model Builder Canvas with two table cards already placed: "orders" (fact type, blue badge) and "dim_product" (dim_aggregate type, green badge). The Drawer panel on the right shows the schema browser with the "public" schema expanded, listing source tables. The table "dim_date" is highlighted and ready to be added. The Table type selector shows "dim_aggregate" selected and the "Add to Model" button is visible below. Dimensions: 1280x800px.