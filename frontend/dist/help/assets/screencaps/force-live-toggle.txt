Screenshot of the Measure Query Panel top-right control cluster. A Force Live toggle sits next to the Execute button. The toggle is in the process of being switched on — half-way through the animation — and a tooltip overlay above it reads "Force Live bypasses aggregate and pocket routing for this query only. Row security still applies. Resets on panel reload." Behind the toggle the Route badge above the grid updates in real-time from "aggregate" (green) to "live" (red) with an arrow animation. Dimensions: 900x500px. Purpose: make clear that Force Live is a one-query investigative tool, not a default setting.
