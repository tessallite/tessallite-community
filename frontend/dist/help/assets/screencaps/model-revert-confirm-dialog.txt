Screenshot of the typed-name confirmation dialog that appears when the user clicks Revert on an older version. Title: "Revert to v1?". Body explains that reverting will replace the live state with the chosen version's snapshot AND delete every newer version. A warning callout reads "This deletes versions v2, v3, ... There is no undo.". A TextField labeled "Type 'revert to v1' to confirm" is partially filled. The destructive "Revert" button is disabled until the typed text matches; show it in the disabled state. Cancel button on the left. Dimensions: 600x400px.
