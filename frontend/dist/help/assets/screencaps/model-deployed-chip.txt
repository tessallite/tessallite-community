Screenshot of the Explorer page Models grid for a project with at least 3 models in different deploy states. Each model Card shows: the StorageIcon, model display name, a Chip next to the name reading either "Deployed" (filled green) or "Ready" (outlined grey), the slug below, and a "last deployed YYYY-MM-DD HH:MM" caption when deployed. Below the card the Enabled/Disabled switch and the actions menu IconButton. At least one Deployed and one Ready card visible side-by-side so the contrast is clear. Crop the model grid only — no project sidebar, no header. Dimensions: 1280x500px.
