Screenshot of a code editor (or terminal `cat`) showing the `.env` file with the bootstrap variables that hold credentials. Visible lines: CREDENTIAL_ENCRYPTION_KEY=..., JWT_SECRET_KEY=..., POSTGRES_PASSWORD=..., SYSTEM_ADMIN_PASSWORD=..., and an OPENAI_API_KEY=... or ANTHROPIC_API_KEY=... example near the bottom. Comments above each variable explain what it is. The file is plain text; all values shown are illustrative placeholders, never real credentials. Dimensions: 1000x700px.
