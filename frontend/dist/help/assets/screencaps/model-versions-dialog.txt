Screenshot of the Versions dialog opened from the Model Builder toolbar (clock icon). The dialog shows a Material-UI Dialog with title "Versions" and a table of saved versions. Columns: Version (e.g. "v3"), Created (relative time + absolute), By (user email), Summary (optional commit-message text), Deployed (green check chip on the deployed row), Actions (Deploy + Revert IconButtons per row). At least 3 rows visible. Pick a model with multiple saved versions and one currently deployed. Crop tightly around the dialog (no surrounding builder UI). Dimensions: 900x600px.
