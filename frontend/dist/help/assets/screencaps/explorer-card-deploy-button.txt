Screenshot of the project Explorer page showing two model cards side by side. The left card is a DEPLOYED model — its header shows the model name, a green "Deployed v3" chip, and an outlined "Undeploy" button in the card's action row. The right card is a READY (undeployed) model — its header shows the model name, a neutral "Ready" chip (no version), and a filled (contained) "Deploy" button in the card's action row. Other card actions (Edit, Delete, etc.) stay visible on both cards so the reader can see where the new Deploy/Undeploy button sits among them. Capture on the default theme at 1440x900, then crop to ~1100x420 so both cards are visible without extra whitespace.
