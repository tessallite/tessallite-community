Screenshot of the Measure Query Panel, full-app layout 1440x900px. Left Toolbelt shows icons (Canvas, Measures, Dimensions, Joins, Aggregates, Pockets, Row Security, Measure Query — this icon highlighted). Centre layout: header strip with model name "retail_demo", Save / Deploy buttons and a Deployed chip. Below the header, a control row with four dropdowns side by side — "Measure: Revenue (currency_eur)", "Row dimensions: region, quarter" (two chips stacked), "Column dimensions: product_category" (one chip), "Totals: subtotals + grand totals" (toggle). A green Run button, a Force Live toggle (off), and an Export split-button (CSV / JSON) sit at the right end. Body shows a pivot grid 680 px tall: three row-dimension columns on the left (region, quarter + subtotals row), five measure columns across the top (product_category A, B, C, D, grand total), cells formatted as "€1 234 567" with right alignment. A yellow Route badge above the grid reads "aggregate · rev_by_region_quarter" with tooltip preview. A chip row below the badge shows "rows: 720 · ms: 41 · bytes scanned: 180 KB". Bottom of the panel, a slicer bar with three chips — "year = 2024", "channel in (online, retail)", "currency = EUR" — each chip has an X to remove. Dimensions: 1440x900px. Purpose: orient a business user to the panel's full capability in a single frame.
