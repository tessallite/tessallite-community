Screenshot of the model canvas with the Measure Query tool open in the right panel. The top navigation shows ModelX with Canvas selected. The canvas contains two joined table cards. The right panel is titled "Measure Query" and shows the empty setup controls: a Measure selector, Run button, Rows and Columns add controls, Execution Mode, and Add Filter. No query has been configured or run, so the screenshot does not contain a result grid or drill-through rows. Dimensions: 1280x860px. Purpose: show where an analyst starts a measure query before selecting a result cell for inline drill-through.
