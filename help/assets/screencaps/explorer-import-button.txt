Screenshot of the Explorer page (Models view) for a project that already has at least one model. The header bar shows the project title at left, then a 3-dot menu IconButton, then the green "Add Model" Button (with plus icon), then the outlined "Import" Button (with upload icon). Crop the top quarter of the page so the Models grid below is just visible at the bottom of the frame. Dimensions: 1280x320px.
