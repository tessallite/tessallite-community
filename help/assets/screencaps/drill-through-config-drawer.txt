Screenshot of the Drill-through Configuration sub-drawer, opened from the Measure form's "Drill-through columns" button. Right-anchored 520 px sub-drawer. Header reads "Drill-through · revenue". Body: a "Source fact table" read-only row showing "public.orders" with a linked tag. A "Detail columns" multi-select with 7 columns checked (order_id, order_date, region_code, product_id, quantity, net_sales, margin) out of 14 available — an "(all columns)" radio above it offers the default. A numeric "Row limit override" field, blank with placeholder "Default 1 000 (max 10 000)". A help-tip card reads "Leaving detail columns blank returns every column of the source fact. Configure this only when the fact has PII or rarely-needed columns you want to hide from drill-through consumers." Footer: Cancel / Save. Dimensions: 1024x700px. Purpose: show the modeller where to restrict the column set and where the row-limit override lives.
