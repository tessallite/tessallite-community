Screenshot of the System Configuration page with the "Environment" tab selected. A two-column grid lists each environment variable: the name in a monospace font on the left (SYSTEM_DATABASE_URL, CREDENTIAL_ENCRYPTION_KEY, JWT_SECRET_KEY, JWT_ALGORITHM, SYSTEM_ADMIN_EMAIL, SYSTEM_ADMIN_PASSWORD, JDBC_PORT, XMLA_PORT, QUERY_ROUTER_URL, MODEL_SERVICE_URL, OPTIMIZER_URL, CORS_ORIGINS); the right column shows the value (secrets shown as "***** (44 chars)" with the character count, non-secrets shown verbatim, the DSN with password redacted as "postgresql+asyncpg://tessallite:*****@postgres:5432/tessallite_system"). A short description in muted text below each value explains what it does. No edit buttons appear — the tab is read-only. Dimensions: 1280x600px.
