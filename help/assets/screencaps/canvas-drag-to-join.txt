Screenshot of the Drag-to-Join v2 interaction mid-gesture. The "orders" fact node and the "customer" dimension node are visible side-by-side. The user has clicked and started dragging from the orders.customer_id column cell; a ghost line trails the cursor toward the customer.customer_id column cell, which is highlighted with a green acceptance ring. Above the drag, a small tooltip reads "Join: orders.customer_id = customer.customer_id · type: many-to-one · inferred". A small secondary tooltip further down the drag path reads "Release on the target column to confirm." Any non-matching column cells on the customer node are dimmed. Dimensions: 1280x720px. Purpose: show the modeller the exact gesture for Drag-to-Join v2 including the inferred join type.
