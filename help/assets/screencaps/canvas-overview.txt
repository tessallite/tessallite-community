Screenshot of the full Model Canvas view inside Model Builder. Full app 1440x900px. Left Toolbelt visible with Canvas icon highlighted. Centre body is a dark-neutral canvas showing a star-schema arrangement: at the centre a large fact node "orders" with three column segments (Measures: revenue · margin · quantity — blue strip at top; Dimensions-in-use: region_code · product_id · order_date — green strip in middle; Unused: internal_id · legacy_note — grey strip at bottom, collapsed). Four dimension nodes surround the fact: "region" top-left, "product" top-right, "customer" bottom-left, "date" bottom-right. Joins rendered as labelled lines ("orders.region_code = region.region_code"). The "date" node shows a pill "hierarchy: date > quarter > month > day" overlaid at its top. The "orders" node carries two overlay chips top-right — a green "agg · rev_by_region_quarter" and a purple "pocket · margin_scoped_q1". In the bottom-right corner a minimap (280x180 px) renders the whole model reduced, with a scroll rectangle showing the current viewport. Bottom status bar shows "5 tables · 3 joins · 2 aggregates · 1 pocket · health: green · last saved 2 min ago". Dimensions: 1440x900px. Purpose: orient a modeller to every Phase 6.A feature in a single frame.
