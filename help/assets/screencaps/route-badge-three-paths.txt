Illustration (diagram, not a live screenshot) showing the three route paths a query can take, rendered in the Tessallite palette. Top of the frame: a browser window labelled "BI tool / Measure Query Panel / API caller" sends a single arrow downward labelled "query". The arrow hits a large rounded box labelled "Query Router" in the centre. Three labelled arrows leave the Router: (1) a green arrow to a cylinder labelled "Aggregate · rev_by_region_quarter" → cell returned in "41 ms"; (2) a blue arrow to a cylinder labelled "Pocket · curated_margin_view" → cell returned in "62 ms"; (3) a red arrow to a cylinder labelled "Source · public.orders (20 M rows)" → cell returned in "1 840 ms". Next to each arrow, a Route badge chip is rendered in the matching colour with the route name. Below the three cylinders, a caption bar reads "The Route badge on the result surfaces which path served the query, and the tooltip explains why." Dimensions: 1200x700px. Purpose: make routing mental-model legible to a business user who has never thought about pre-computed aggregates.
