Screenshot of the inline measure-format editor, opened from clicking the format indicator (e.g. "€ 2dp") on a measure-column header chip. A compact popover appears anchored below the chip with four rows: a Format preset dropdown set to "Currency · EUR · 2 decimals" showing options like "Number", "Percent", "Currency", "Compact (K/M/B)"; a Decimals spinner "2" with +/- buttons; a Thousand-separator toggle on; a Preview row rendering "1234567.891" in the selected format ("€1,234,567.89"). A small "Apply to this column only" vs "Save on measure" radio pair at the bottom. Dimensions: 760x480px. Purpose: show business users that the number formatting seen across every BI tool can be tweaked in place without rebuilding the measure.
