Screenshot of the Calculated-measure Decomposed Drill Drawer opened from a cell of the calculated measure "Gross margin %" (31.4%) at (region=EMEA, quarter=2024-Q2). Right-anchored 780 px drawer. Header reads "Drill-through · Gross margin % · EMEA · 2024-Q2". Top band shows a bordered read-only "Calculated value" card: a big "31.4%" figure and, beneath it in monospace small caps, the raw expression "safe_div(measure(\"gross_margin\"), measure(\"net_sales\"))". Below the card, two stacked mini drill-panels: the first titled "gross_margin (numerator) — 312 matching rows" with its own 50-row page, a pagination footer "1-50 of 312 · Next" and a small "Load more" button; the second titled "net_sales (denominator) — 312 matching rows" with its own 50-row page and own Next button. A dashed separator sits between them. Dimensions: 1280x900px. Purpose: make explicit that calculated measures are drillable by decomposing into their base measures, each paginated independently.
