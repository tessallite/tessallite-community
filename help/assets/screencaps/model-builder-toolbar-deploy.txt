Screenshot of the Model Builder top toolbar after a Save click. The status Chip reads "Saved v3" in default colour. The Deploy IconButton (rocket icon, success green) is highlighted/focused. To its right the Versions IconButton (clock icon) and Export IconButton (file-download icon) are visible. Pick a model that has at least one saved version. Crop tightly around the toolbar (no Canvas, no Drawer, no Toolbelt). Dimensions: 1280x180px.
