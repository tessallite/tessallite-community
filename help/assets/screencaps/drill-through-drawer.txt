Screenshot of the Drill-through Drawer opened from a cell click in the Measure Query Panel. Right-anchored 780 px drawer. Header reads "Drill-through · Revenue — Q2 2024 · region = EMEA" with a close X. Below the header a compact row of chips shows the cell coordinates: "measure: revenue", "quarter: 2024-Q2", "region: EMEA". A fact-table preview panel lists the columns returned (order_id, order_date, region, product_id, quantity, net_sales, margin) with their data-type pills (int, date, varchar, int, numeric(10,2), numeric(10,2)). Below, a dense table of 25 rows with zebra striping, right-aligned numerics, and the order_id link-styled for future deep-link. A pagination footer reads "Showing 1-25 of 312 · rows scanned 312 · 48 ms" with "Load more" and "Export CSV" buttons on the right. A small Route badge top-right reads "live" with a tooltip sliver "No aggregate covers this grain". Dimensions: 1280x860px. Purpose: show business users what drill-through actually returns and reassure them the cell they clicked is traced to identifiable source rows.
